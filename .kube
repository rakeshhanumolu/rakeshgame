mkdir -p ~/.kube                                  # Create the .kube directory if it doesn't exist
echo "***" | base64 --decode > ~/.kube/config     # Decode the base64 string and write to config
chmod 600 ~/.kube/config                          # Set the correct permissions for the config file
